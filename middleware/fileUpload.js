const { S3Instance } = require("../config/aws.config");
const { PutObjectCommand } = require("@aws-sdk/client-s3");

async function uploadFile() {
  try {
  } catch (error) {
    console.log("err", error);
  }
}
